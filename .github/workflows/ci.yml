name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-rust:
    name: Build Rust
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Cache system dependencies
      uses: actions/cache@v4
      with:
        path: |
          /var/cache/apt/archives
          ~/.cargo/registry
          ~/.cargo/git
        key: system-deps-${{ runner.os }}-${{ hashFiles('**/Cargo.toml', '**/Cargo.lock') }}
        restore-keys: |
          system-deps-${{ runner.os }}-

    - name: Install Protocol Buffers and Thrift
      run: |
        sudo apt-get update
        sudo apt-get install -y protobuf-compiler thrift-compiler build-essential netcat-openbsd librocksdb-dev cmake libc6-dev

    - name: Set up Rust
      uses: actions-rust-lang/setup-rust-toolchain@v1
      with:
        toolchain: stable
        cache: false  # We'll handle caching manually for better control

    - name: Cache Rust build artifacts
      uses: actions/cache@v4
      with:
        path: |
          ~/.cargo/registry/index/
          ~/.cargo/registry/cache/
          ~/.cargo/git/db/
          rust/target/
        key: rust-build-${{ runner.os }}-${{ hashFiles('rust/**/Cargo.lock', 'rust/**/Cargo.toml') }}-${{ hashFiles('rust/src/**/*.rs', 'rust/crates/**/*.rs') }}
        restore-keys: |
          rust-build-${{ runner.os }}-${{ hashFiles('rust/**/Cargo.lock', 'rust/**/Cargo.toml') }}-
          rust-build-${{ runner.os }}-

    - name: Cache CMake build directory
      uses: actions/cache@v4
      with:
        path: |
          build
          rust/src/generated
          nodejs/generated
        key: cmake-build-${{ runner.os }}-${{ hashFiles('CMakeLists.txt', 'thrift/**', 'proto/**') }}
        restore-keys: |
          cmake-build-${{ runner.os }}-

    - name: Cache GoogleTest
      uses: actions/cache@v4
      with:
        path: |
          build/_deps/googletest-*
        key: googletest-${{ runner.os }}-release-1.12.1
        restore-keys: |
          googletest-${{ runner.os }}-

    - name: Build everything with CMake
      run: |
        cmake -B build -S .
        cmake --build build

    - name: Start shard server for tests
      run: |
        ./build/bin/shard-server &
        # Wait for server to start
        for i in {1..30}; do
          if nc -z localhost 9090 2>/dev/null; then
            echo "Server is ready"
            break
          fi
          sleep 1
        done

    - name: Run FFI tests
      run: |
        ./build/bin/cpp_ffi_test

    - name: Run Rust tests
      run: |
        cd rust
        # Run tests without RSML features
        cargo test --workspace --no-default-features --features ffi
